@page
@model WebApplication.Pages.Admin.Orders.EditModel
@{
    bool isEdit = Model.Order.Id > 0;
    ViewData["Title"] = isEdit ? ("Редактировать заказ #" + Model.Order.Id) : "Создать заказ";
}

<h3 class="text-center">@ViewData["Title"]</h3>
<hr />

